// Rules to declare Variables

// No keyword in variable name
var false=40;

// No space or gape in variable name
var is happy=true;

// No quote
var "address"="abs";

// Can't start with number but other than the first letter is allowed
var 1money=100000;

// Name is case secsitive
var abc;
var aBC; Both are different;